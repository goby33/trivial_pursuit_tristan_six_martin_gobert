import 'package:flutter/material.dart';

class Classement extends StatelessWidget {
  Classement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Classement");
  }
}
