import 'package:flutter/material.dart';

class GamePageScore extends StatelessWidget {
  GamePageScore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("ppp");
  }
}
